// Testing: everything together (relational + logical + loop + nesting)

int a = 5;
int b = 2;

if (a > 2 && b != 0) {
    while (a > 0 || b == 2) {
        print(a);
        a = a - 1;
        b = b - 1;
    }
}